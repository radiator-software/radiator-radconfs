# 20_statistics.radconf
#
# Radiator statistics configuration
#

statistics {
    defaults {
        # interval in ms, 60000ms = 60s
        interval 60000;
        # 1440 = 24*60 is 24h of statistics
        samples 1440;
    }
}
