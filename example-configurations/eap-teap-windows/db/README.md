# Databases for example configurations

These are the databases for the example configurations.  They are intended to be installed under /var/lib/radiator/db/ directory in the similarly named directories.  For example the files under users-internal directory are to be installed under /var/lib/radiator/db/users-internal directory.
